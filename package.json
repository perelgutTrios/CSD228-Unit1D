{
  "name": "simple-calculator-tests",
  "version": "1.0.0",
  "description": "Test suite for Simple Calculator Application",
  "main": "script.js",
  "scripts": {
    "start": "echo Starting Simple Calculator... && start index.html",
    "serve": "python -m http.server 8080 || python3 -m http.server 8080",
    "pwa-test": "echo Testing PWA functionality... && start http://localhost:8080",
    "mobile-guide": "start MOBILE_CONVERSION_GUIDE.md",
    "test": "jest --testPathPattern=tip-calculator",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage --coverageReporters=text-lcov --coverageReporters=html",
    "test:ui": "jest --testPathPattern=ui",
    "test:unit": "jest --testPathPattern=tip-calculator",
    "test:integration": "jest --testPathPattern=tip-calculator", 
    "test:all": "jest --testPathPattern=tip-calculator",
    "test:ci": "jest --coverage --watchAll=false --ci"
  },
  "keywords": [
    "calculator",
    "javascript",
    "testing",
    "jest"
  ],
  "author": "CSD228 Student",
  "license": "MIT",
  "devDependencies": {
    "@testing-library/jest-dom": "^6.1.4",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "setupFilesAfterEnv": [
      "<rootDir>/tests/setup.js"
    ],
    "collectCoverageFrom": [
      "script.js",
      "!node_modules/**"
    ],
    "coverageReporters": [
      "text",
      "lcov",
      "html"
    ],
    "testMatch": [
      "<rootDir>/tests/**/*.test.js"
    ]
  }
}
